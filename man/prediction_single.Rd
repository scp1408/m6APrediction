% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_single}
\alias{prediction_single}
\title{Single-Sample Prediction of m6A Sites}
\usage{
prediction_single(
  ml_fit,
  gc_content,
  RNA_type,
  RNA_region,
  exon_length,
  distance_to_junction,
  evolutionary_conservation,
  DNA_5mer,
  positive_threshold = 0.5
)
}
\arguments{
\item{ml_fit}{Pre-trained machine learning model (e.g., random forest model).}

\item{gc_content}{Numeric value, representing GC content.}

\item{RNA_type}{Character string, specifying RNA type (allowed values: mRNA/lincRNA/lncRNA/pseudogene).}

\item{RNA_region}{Character string, specifying RNA region (allowed values: CDS/intron/3'UTR/5'UTR).}

\item{exon_length}{Numeric value, representing exon length.}

\item{distance_to_junction}{Numeric value, representing distance to junction.}

\item{evolutionary_conservation}{Numeric value, representing evolutionary conservation.}

\item{DNA_5mer}{Character string, 5-nucleotide DNA sequence (containing only A/T/C/G).}

\item{positive_threshold}{Numeric value (0-1) for positive status cutoff, default = 0.5.}
}
\value{
Named vector with two elements: predicted_m6A_prob (predicted probability), predicted_m6A_status (predicted status).
}
\description{
Predicts m6A site probability and status for a single sample using a pre-trained machine learning model.
}
\examples{
# 1. Load pre-trained random forest model from the package
trained_model <- readRDS(
  system.file("extdata", "rf_fit.rds", package = "m6APrediction")
)

# 2. Run single-sample prediction (input valid feature values manually)
single_sample_pred <- prediction_single(
  ml_fit = trained_model,
  gc_content = 0.62,
  RNA_type = "mRNA",          # Must be one of: mRNA/lincRNA/lncRNA/pseudogene
  RNA_region = "CDS",         # Must be one of: CDS/intron/3'UTR/5'UTR
  exon_length = 10.8,
  distance_to_junction = 8.9,
  evolutionary_conservation = 0.75,
  DNA_5mer = "ATCGG",         # Must be a 5-nucleotide string (A/T/C/G only)
  positive_threshold = 0.5
)

# 3. View prediction results (named vector: probability + status)
print(single_sample_pred)
}
